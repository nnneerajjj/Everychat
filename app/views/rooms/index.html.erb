<table class="pure-table pure-table-bordered">
  <thead>
    <tr>
      <th>Room name</th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @rooms.each do |room| %>
    <tr>
      <td>
        <%= room.name %>
      </td>
      <td>
        <%= link_to 'Enter', enter_room_path(room), method: :put, class: 'button-green pure-button' %>
      </td>
      <td>
        <%= link_to 'Delete', room, method: :delete, class: 'button-red pure-button' %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= form_for @room, html: { class: 'pure-form' } do |f| %>
  <fieldset>
    <legend>Create new room</legend>
    <%= f.text_field :name %>
    <%= f.submit 'Create', class: 'pure-button pure-button-primary' %>
  </fieldset>
<% end %>
